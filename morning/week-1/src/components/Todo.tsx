// imports the entire react module

// .map(() => )
// .filter(() => )
// .reduce(() => )

type TodoType = {
  description: string;
  due_date: string;
};

export const Todo = (props: TodoType) => {
  return (
    <>
    </>
  );
};
